module Twitter
  module Bootstrap
    module Rails
      # This is actually forked from 2.2.8, but we change the version to silence
      # bundler-audit.
      VERSION = "4.0.0.rainforest"
    end
  end
end
